# Git hooks

## Overview

This directory contains one pre-commit git hook and one pre-push git hook.

The aim of these hooks is to format, lint and validate the code of the repository during its development.

## Installation on Linux

To set the git hooks properly give execution rights to the set-hooks.sh file and then run it:

~~~
chmod +x ./set-hooks.sh
./set-hooks.sh
~~~
